# streamlit-drawable-canvas 0.9.3 imports streamlit.elements.image.image_to_url,
# which Streamlit removed in 1.41.0 (present in 1.40.0, gone in 1.41.0). Pinned
# EXACTLY so dev and the Windows CI build resolve to the identical version — a
# range let CI grab a newer, broken release (1.42/1.43) while older dev envs kept
# working, so the shipped exe crashed on the first canvas render.
streamlit==1.40.0
streamlit-drawable-canvas>=0.9.3
streamlit-aggrid>=1.0.5,<2.0.0
PyMuPDF>=1.24.0
# Pin the 2.x line: img2table 2.0 dropped polars/numba for a lighter stack
# (pypdfium2 + opencv). Leaving this unpinned caused a dev/CI skew (1.4.x vs
# 2.0.0) with different deps. Keep dev and the Windows build on the same major.
img2table>=2.0.0,<3.0.0
pandas>=2.0.0
pillow>=10.0.0
